#include <stdio.h>
/**
*main - Entry point
*Description: lower case
*Return: Always 0 (Success)
*/
int main(void)
{
for (char start = 'a'; start <= 'z'; start++)
{
putchar(start);
putchar('\n');
}
return (0);
}
